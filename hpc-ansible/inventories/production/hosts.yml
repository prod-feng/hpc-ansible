all:

  children:

    controller:

      hosts:

        head01:

    login:

      hosts:

        login01:

        login02:

    compute:

      hosts:

        compute[001:500]:

    gpu:

      hosts:

        gpu[001:032]:

